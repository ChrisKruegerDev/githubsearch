<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="searchViewStyle">@style/AppSearchView</item>

        <item name="colorIcon">@color/grey_500</item>

        <item name="android:textColorSecondary">@color/grey_500</item>

        <item name="colorBackgroundPlaceholderDark">@color/blue_grey_300</item>
        <item name="colorBackgroundPlaceholderInline">@color/grey_50</item>
    </style>

    <style name="AppSearchView" parent="@style/Widget.AppCompat.SearchView">
        <item name="searchHintIcon">@null</item>
        <!-- Background for the search query section (e.g. EditText) -->
        <item name="queryBackground">@null</item>
        <!-- Background for the actions section (e.g. voice, submit) -->
        <item name="submitBackground">@null</item>
    </style>

</resources>
